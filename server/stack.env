# My Sister's Closet - Label Maker Environment Variables
# This file is used by Portainer for Git repository deployments
# Override these values in Portainer's environment variables section

# Server Configuration
NODE_ENV=production
PORT=5002
SERVER_PORT=5002
CLIENT_PORT=80

# JWT Secret - CHANGE THIS in Portainer to a secure random value
# Generate with: openssl rand -base64 64
JWT_SECRET=changeme

# Email Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Client URL - Update this with your server's domain or IP
CLIENT_URL=http://localhost